function total(){
	// 合计公式，返回极限值
	var startDate;
	var endDate;
	var allDate = startDate - endDate;
	var yongJin;
	// 最大支出
	var _max; // 循环后得出的最大和
	var _min; // 循环后得出的最小和
	var _mini; // 配置项中的最小金额
	var Max = (_max * (1+yongJin) - (_mimi*yongJin));
	return new array(Max, _min);
}
